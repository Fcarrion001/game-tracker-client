
<h3 class="game-name">{{game.game_name}}</h3>
<h4>Summary</h4>
{{#if game.summary}}
      <p>{{game.summary}}</p>
    {{else}}
      <p>No summary provided.</p>
    {{/if}}
<h4>Storyline</h4>
{{#if game.storyline}}
      <p>{{game.storyline}}</p>
    {{else}}
      <p>No storyline provided.</p>
    {{/if}}
<img src="https://images.igdb.com/igdb/image/upload/t_cover_big/{{game.cloudinary_id}}.jpg" alt="screen shot">

<h4 class="message hbs-message"></h4>

{{!-- <form id="post-wanted-game">
  <fieldset>
    <input type="hidden" name="wanted_game[game_id]" value='{{game.id}}'>
    <input type="submit" name="submit" value="Add to wishlist">
  </fieldset>
</form> --}}

<button id="post-wanted-game" type="button" class="btn btn-success" name="post-wanted-game-btn">Add to Wishlist</button>
<button type="button" class="back-btn btn btn-secondary" name="back-button">Back</button>
